= simple_form_for (@resource) do |f|
  - if @resource.new_record?
    %p
      = f.input :title, as: :string, label: 'Уникальное значение для создания понятного человеку адреса магазина (например, webmall.ru/stores/example вместо webmall.ru/stores/9832471271)'
  %br
  %p
    = f.input :name, label: 'Название'
  %p
    = f.input :type, label: 'Специализация'
  %p
    = f.input :store_country, label: 'Страна'#, collection: @countries_list
  %p
    = f.input :store_city, label: 'Город'
  %p
    = link_to "Назад", profile_path(@user.profile), class: "btn btn-default"
    = f.button :submit, 'Сохранить', class: "btn btn-primary"